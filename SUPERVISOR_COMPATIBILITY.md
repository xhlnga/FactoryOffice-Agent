# FactoryOffice-Agent 业务与管理层说明

## 1. 文档目的

本文档用于帮助业务负责人、管理人员、信息化负责人和技术负责人快速理解 FactoryOffice-Agent 的业务价值、适用范围、运行机制和风险边界。

本文档不替代项目唯一真源。项目事实、技术路线和功能边界以 `00_project_truth/PROJECT_SSOT.md` 为准。

## 2. 项目解决什么问题

制造业企业通常有大量制度、流程和操作资料，例如采购制度、报销制度、设备维修手册、质量异常处理流程、安全生产规范、项目周报模板等。

这些资料是企业正常运行的基础，但在日常工作中经常出现以下问题：

- 员工不知道应该查哪份制度或手册。
- 找到文档后，难以快速定位关键条款。
- 设备异常、质量异常、采购申请等流程依赖个人经验。
- 会议纪要需要人工整理成任务，容易遗漏责任人、截止时间和优先级。
- AI 如果只回答问题、不提供来源、不经过审批、不留下日志，很难用于正式业务。

FactoryOffice-Agent 的目标是把这些分散、重复、容易遗漏的办公动作整合成一个可控入口：

```text
员工输入问题或业务描述
        ↓
系统识别是查制度、生成任务、生成工单、生成采购申请还是写周报
        ↓
系统检索企业知识库或进入固定流程
        ↓
生成可复核的回答或业务草稿
        ↓
正式写入前由人工确认
        ↓
执行后留下审计记录
```

让 AI 帮助员工更快准备材料、减少重复整理、降低漏项风险，并把关键动作留在可追踪的流程里。

## 3. 为什么这个项目符合制造业企业实际

### 3.1 制造业的知识和流程天然适合这种系统

制造业企业的业务知识通常沉淀在制度、SOP、设备手册、质量规范和项目文档中。员工问的问题往往不是开放闲聊，而是非常具体：

```text
采购超过 5 万需要谁审批？
空压机 E07 报警应先排查什么？
这批零件尺寸超差，质量异常流程怎么走？
临时动火作业是否需要审批？
周报里质量风险和采购风险怎么写？
```

这些问题需要“查企业资料 + 结合流程 + 给出可追溯依据”，而不是让模型凭经验直接回答。

### 3.2 系统不是直接替人做决定

项目设计中，AI 只负责辅助：

- 查询知识库。
- 提炼信息。
- 生成草稿。
- 提醒缺失字段。
- 形成待确认动作。

正式动作仍然需要人工确认：

- 创建任务需要审批。
- 提交维修工单需要审批。
- 提交质量异常工单需要审批。
- 提交采购申请需要审批。
- 删除知识库文档需要审批。

这种设计符合企业对责任边界的要求。系统不会把安全、质量、预算、交付等关键事项交给 AI 自动决定。

### 3.3 业务对象和企业系统结构接近

项目不是只有聊天记录，而是有明确业务对象：

- 文档
- 文档切块
- 任务
- 工单
- 采购申请
- 审批记录
- 审计日志
- 用户和角色

这些对象与企业内部常见系统的结构相近。后续接入真实 OA、ERP、MES、飞书、钉钉或企业微信时，可以把当前模拟表和工具层替换为真实接口。

### 3.4 有审计记录，便于管理复盘

系统记录关键过程：

- 用户输入了什么。
- 检索了哪些资料。
- 生成了什么草稿。
- 准备调用什么工具。
- 谁批准或拒绝。
- 执行结果是什么。

这对企业非常重要。没有审计日志的 AI 办公系统，出了问题难以追踪责任，也难以改进流程。

## 4. 当前可以帮助企业完成哪些工作

### 4.1 企业制度和流程问答

员工可以用自然语言查询企业制度和流程。

典型问题：

```text
采购金额超过 5 万需要谁审批？
差旅报销需要哪些附件？
质量异常发生后第一步要做什么？
安全生产规范里哪些作业必须提前审批？
```

系统会检索知识库文档，返回回答和引用来源，便于员工回查原始文件。

### 4.2 设备异常处理辅助

员工输入设备异常后，系统可以生成维修工单草稿。

典型输入：

```text
A3 产线空压机 E07 报警，末端压力波动，产线暂停 35 分钟。
```

系统可以输出：

- 工单类型。
- 工单标题。
- 优先级。
- 异常描述。
- 初步排查建议。
- 是否需要人工确认。

正式提交维修工单前仍需负责人确认。

### 4.3 质量异常处理辅助

员工输入质量异常描述后，系统可以生成 NCR 或质量异常工单草稿。

典型输入：

```text
B2 批次零件抽检发现 3 件尺寸超差，影响当前批次出货。
```

系统可以辅助整理：

- 异常类型。
- 影响范围。
- 初步处置建议。
- 后续需要质量人员确认的事项。
- 是否进入质量异常工单审批。

系统不会直接判定最终责任，不会直接决定放行、返工或报废。

### 4.4 采购申请草稿

员工描述采购需求后，系统可以提取采购申请字段：

- 物品名称
- 数量
- 采购原因
- 预算
- 供应商

如果信息不完整，系统会提示补充字段。字段完整后，系统生成采购申请草稿，并进入审批。

### 4.5 会议纪要转任务

系统可以从会议纪要中提取任务。

示例：

```text
张工周五前完成设备巡检方案；
李工下周一前联系供应商确认交期；
王经理负责审批采购预算。
```

系统会生成任务草稿，包括任务标题、负责人、截止时间、优先级和描述。批量创建任务前需要人工确认。

### 4.6 项目周报草稿

系统可以根据项目记录生成周报草稿，包括：

- 本周进展
- 本周问题
- 质量风险
- 设备风险
- 采购风险
- 下周计划
- 需要管理层决策事项

周报草稿需要项目负责人复核后使用。

## 5. 当前系统运行方式

项目通过 Docker Compose 启动以下服务：

- PostgreSQL + pgvector
- FastAPI 后端
- Vue3 前端

启动后可以访问：

```text
前端：http://localhost:3000
后端：http://localhost:8000
接口文档：http://localhost:8000/docs
```

系统内置演示文档和业务数据，便于在本地快速验证知识库、流程、审批和审计能力。

## 6. 当前数据和流程为什么可信

本项目使用的是模拟数据，但模拟方式尽量贴近企业实际：

- 文档类型覆盖采购、报销、设备维修、质量异常、安全生产和项目周报。
- 业务数据覆盖用户、任务、工单、采购申请、审批和审计日志。
- 流程上区分“草稿生成”和“正式写入”。
- 高风险动作必须审批。
- 删除文档使用审批和软删除逻辑，而不是直接硬删除。
- 审批接口和审计日志接口需要主管或管理员角色。
- 未配置真实大模型或 Embedding API 时，系统提供本地兜底能力，保证工程链路可运行。

因此，这个项目不是演示一个孤立的 AI 问答页面，而是在模拟一个企业 AI 办公系统应具备的基本闭环。

## 7. 管理层应重点关注的价值

### 7.1 降低知识查询成本

员工不需要反复翻文件、问同事或依赖经验查制度。系统可以帮助定位相关资料，并提供引用来源。

### 7.2 提高流程材料完整性

采购申请、维修工单、质量异常单、会议任务和周报都需要字段完整。系统可以先生成草稿并提示缺失信息，减少反复沟通。

### 7.3 保持人工控制

系统不直接替企业做关键决定。它把 AI 生成内容放在审批前，让人确认后再执行。

### 7.4 便于审计和复盘

所有关键动作都有输入、参数、审批和执行结果，便于管理人员回看流程问题。

### 7.5 后续可逐步接入真实系统

当前先用数据库模拟业务系统，验证流程闭环。后续可以逐步接入真实 OA、ERP、MES、即时通讯平台或企业知识库。

## 8. 当前限制

为保证项目边界清晰，当前版本有以下限制：

- 当前不直接接真实 ERP、MES、OA、飞书、钉钉或企业微信。
- 当前不处理真实企业内部敏感文件。
- 当前权限是本地角色模拟，不是生产级统一身份认证。
- 当前工作流是固定业务流程，不是完全自由行动的 Agent。
- 当前本地演示向量只用于无 API Key 场景，正式知识库应配置真实 Embedding 服务。
- 当前质量、安全、财务等结论只能作为草稿和参考，最终仍需责任人确认。

这些限制是有意设计的。企业级 AI 系统不应一开始就让 AI 自由接管所有动作，而应先在可控范围内验证知识库、流程、审批和审计闭环。

## 9. 推荐评估方式

如果要判断这个代码库是否值得技术团队深入查看，可以按以下顺序评估：

1. 查看 `PROJECT_SSOT.md`，确认项目目标、范围和边界。
2. 启动 Docker Compose，确认前端、后端、数据库可运行。
3. 导入 `data/demo_docs/` 文档，检查文档切块和知识库检索。
4. 在知识库页面提问采购、设备、质量、安全相关问题，检查引用来源。
5. 在流程页面生成会议任务、维修工单、质量异常工单、采购申请和周报草稿。
6. 在审批页面批准或拒绝待执行动作。
7. 在任务、工单、采购页面确认业务对象是否按审批结果写入。
8. 在审计日志页面检查工具参数和执行结果。
9. 查看 `backend/app/services/`、`backend/app/workflows/`、`backend/app/tools/`、`backend/app/agents/`，确认业务逻辑是否清晰可扩展。
10. 查看测试和导入脚本，确认工程是否可复现。

## 10. 适合继续投入的原因

FactoryOffice-Agent 适合继续投入，不是因为它已经等同于生产系统，而是因为它具备企业 AI 办公系统最关键的骨架：

- 有企业知识库。
- 有业务对象。
- 有固定流程。
- 有 Agent 调度入口。
- 有工具层。
- 有人工审批。
- 有审计日志。
- 有前端操作界面。
- 有数据库和迁移。
- 有 Docker 部署。
- 有演示文档和初始化数据。

这些组成部分组合在一起，已经能展示一个制造业企业 AI 办公流程系统的核心形态。后续如果接入真实系统，主要工作会集中在认证、权限、外部系统适配、数据治理、运维安全和流程配置上。

## 11. 结论

FactoryOffice-Agent 是一个面向制造业企业的高度拟真 AI 办公流程工程样板。

它的价值不在于让 AI 随便聊天，而在于把企业知识库、办公流程、人工确认和审计追踪放在同一个系统里，形成一个清晰、可控、可扩展的闭环。

对于需要评估企业 AI 落地方式的团队，这个代码库值得进一步查看其后端结构、数据模型、RAG 模块、工作流模块、审批机制和审计日志设计。
